python -m venv .venv
.venv\Scripts\Activate.ps1  # PowerShell activation
echo $env:VIRTUAL_ENV

pip install -r .\requirements.txt

pip install build
python -m build

pip install twine
twine upload dist/app_common_utils-0.1.0.tar.gz app_common_utils-0.1.0-py3-none-any.whl

python -m twine upload dist/*


python.exe -m pip install --upgrade pip

#### INstall packge locally 

pip install -e .

python.exe test.serice.test_database 
python.exe test.test_package

=======================================
### Set Environment
$env:ENVIRONMENT="production"
$env:ENVIRONMENT="development"

TEST -
=======================================
python -c "import os; print(os.getenv('ENVIRONMENT'))"